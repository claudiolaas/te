"""Integration tests for the trading system.

These tests use real components (databases, external APIs) to validate
end-to-end behavior. They are slower than unit tests but provide more
confidence in the system's correctness.

Run with: pytest -m integration
Skip with: pytest -m "not integration"
"""
